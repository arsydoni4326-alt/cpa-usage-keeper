# Session Log

This file preserves working context between sessions. Update it whenever the project changes meaningfully. Format: newest session at the bottom of the log table, with the "Current state" section always refreshed.

## Current state

- **Latest milestone:** Provider-model **GNN panel now ships with a dual renderer**. `ProviderModelGNNPanel` keeps the existing `@xyflow/react` grid view and adds a header toggle to flip into `ProviderModelReagraphCanvas` (a **reagraph** force-directed WebGL/three.js view) at runtime. The reagraph module is **lazy-loaded** via `lazy(() => import('./ProviderModelReagraphPanel'))` so the ~380 kB gzip three.js bundle only downloads when the user picks the network view — the stand-alone `ProviderModelReagraphPanel` is intentionally *not* re-exported from `components/usage/index.ts` so the chunk actually splits. Both views share one `GET /api/provider-model-gnn` fetch and the `buildProviderModelGraph` join helper, so provider `kind_hash` tinting, `is_shared` badges, feature vectors, and embeddings stay consistent. The reagraph WebGL canvas is **theme-adaptive**: `buildGraphTheme(isDark)` spreads the matching reagraph `lightTheme`/`darkTheme` preset and pins `canvas.background`, node/edge label + sub-label colors, and edge/arrow fills to the exact `--bg-primary`/`--text-*`/`--border-color` hexes, flipping reactively with `data-theme` via the existing MutationObserver. Backend GNN contract unchanged from the 2026-08-03 milestone (6-dim features, one-layer mean message passing, embeddings + meta on `GET /api/provider-model-gnn`).

- **Codebase shape:** Go 1.26+ backend (`cmd/server`, `internal/*`), React 19 + TypeScript SPA in `web/` (Vite 8, SCSS modules, custom design system — no UI framework), embedded into the binary via `web/static.go`.
- **Docs policy:** When modifying features, update the relevant doc in `docs/` and keep `README.md` capabilities in sync (see Project Guidelines).
- **Roadmap:** `docs/ROADMAP.md` holds phased future work (P0 docs/tooling → P4 growth). Before starting roadmap work, re-read that file and tick off steps as they land.

## Key facts to remember

- Storage is **SQLite via GORM + dbresolver** (writer + reader pools). Single-writer discipline: all ingestion writes and derived aggregations are serialized through background runners.
- Ingestion priority: **Redis SUBSCRIBE → Redis list pull → HTTP usage-queue pull** (fallback chain, backoff on failure), landing in `redis_usage_inboxes` inbox before being processed into `usage_events`.
- Derived stats are maintained by one serial `UsageAggregationRunner` with three checkpoints (`overview`, `activity`, `latency`) in `usage_aggregation_checkpoints`; cursors only move monotonically inside transactions.
- Auth has two roles: **admin** (password) and **api-key viewer** (CPA API Key). Sessions may use HttpOnly cookies or per-tab header tokens (CPAMC embed fallback).
- Ranking is opt-in, signed with ed25519 identity, talks to official center `https://keepers.cc.cd` (`/api/v1/leaderboards*`). Failure of ranking must never affect usage collection.
- i18n runs on i18next with **3 locales: en, zh, zh-TW** — every user-facing string must be added to all three.
- Auth is **on by default**: `AUTH_ENABLED` defaults to `true`; unset means `LOGIN_PASSWORD` is required at startup, and the public example password value is rejected.
- Update checker hits the GitHub Releases API; version injected with `-ldflags -X cpa-usage-keeper/internal/version.Version=...`.
- Verification baseline before any PR: `make verify` (or `go test ./...` + npm test/lint/typecheck/build in `web/`).

## Session log

| Date (UTC+7) | Session summary | Artifacts touched |
| --- | --- | --- |
| 2026-08-02 | Scanned whole repository (README, `cmd/server/main.go`, `internal/app/app.go`, code-definition maps of `internal/api` + `internal/entities`, parallel deep-dives into HTTP layer, data pipeline, domain services, frontend, and build/ops). Planned and wrote the complete documentation set: overhauled README, SPEC, ARCHITECTURE, phased ROADMAP, CONFIGURATION reference, CONTRIBUTING. Created this session file. | `README.md`, `docs/*`, `session.md` |
| 2026-08-02 (cont.) | Added a provider ↔ model relationship graph. Backend: `providerconfig.ManagementConfig` DTO parses only whitelisted fields from CPA `/v0/management/config` (api-key/base-url/headers/priority never decoded), `FetchManagementConfig` client method, `NewProviderModelGraphService` + `GET /api/provider-model-graph` route, `provider_model_graph_test.go` (7 tests). Frontend: `fetchProviderModelGraph` helper, `buildProviderModelGraph` (shared models merge), `ProviderModelGraphPanel` using `@xyflow/react`, i18n in 3 locales, rendered in the Usage overview tab after `OverviewRealtimePanel`. Verified: go build/vet/test all green; web typecheck/lint/vitest (105 files / 969 tests) green. | `internal/cpa/dto/providerconfig/*`, `internal/cpa/dto/response/response.go`, `internal/cpa/{endpoints,client}.go`, `internal/service/provider_model_graph*.go`, `internal/api/provider_model_graph.go`, `internal/{api/router,app/app}.go`, `web/src/{lib,components/usage,pages}/*`, `session.md` |
| 2026-08-02 (cont.) | Post-merge documentation sync after reviewing merge `f1f7b0b` from `author/main`. SPEC: added `GET /api/provider-model-graph` to §5.2, new UC-12 provider-topology + renumbered UC-13, §7 whitelisted-fields security note, roles table aligned to AUTH_ENABLED default. ARCH: service list + wiring order now include ProviderModelGraph, OptionalProviders mention, frontend stack documents @xyflow/react graph panel and i18next with en/zh/zh-TW. CONTRIBUTING/ROADMAP: locale lists corrected en/zh → en/zh/zh-TW. CONFIG: fixed `AUTH_ENABLED` default `false` → `true` (matches code `getBool("AUTH_ENABLED", true)` and READMEs). Verified via `internal/config` tests and i18n source. README/README.zh needed no changes (already updated by the merge). | `docs/SPECIFICATION.md`, `docs/ARCHITECTURE.md`, `docs/CONTRIBUTING.md`, `docs/ROADMAP.md`, `docs/CONFIGURATION.md`, `session.md` |

| 2026-08-02 (cont.) | Fixed Provider Model Graph rendering emptiness in production (26+ providers, 130+ models). Root cause: single shared-model column (~6800 px tall) broke React Flow fitView (default minZoom 0.5 clamp). Reworked `buildProviderModelGraph` to a per-provider band layout (one model node per provider-model pair, `model:<provider>::<label>` ids, `edge:...` ids, providerCount shared-label badge data, `totalHeight`), rewrote tests for the new shape. Panel hardened: explicit `minZoom={0.02}`, `fitViewOptions={{padding:0.1, minZoom:0.05, maxZoom:0.9}}`, per-type node styles (fixed widths, ellipsis, disabled variants), hover tooltips with raw name/provider info, summary now reports provider-count and edge-count. i18n summary/subtitle strings updated in en/zh/zh-TW; label datum types widened to `ReactNode`. Verified: web vitest 109 files/989 tests, eslint, and tsc all green. | `web/src/components/usage/{providerModelGraph.ts,providerModelGraph.test.ts,ProviderModelGraphPanel.tsx}`, `web/src/i18n/index.ts`, `session.md` |
| 2026-08-02 (cont.) | Fixed Provider Model Graph still invisible after the band-layout fix (DOM mounted, nodes/edges in tree, but nothing on screen). Root cause: the canvas wrapper had an indefinite flex/min-height chain (`UsagePage.module.scss` `.container > :last-child { min-height:100%; flex:1 0 auto }`, `.canvas { flex:1 1 auto; min-height:420px }`) with no concrete `height`, so React Flow's wrapper could measure 0×0 and fitView silently collapsed the viewport. Fix: `.canvas` now has a definite `height:560px` (keeps `min-height:420px`), decoupling from the fragile chain; `.panel` dropped its min-height. Also hardened the panel: wrapped `<ReactFlow>` in `<ReactFlowProvider>` and added a `FitViewWhenReady` inner component that calls `fitView()` again once `useNodesInitialized()` fires (guards against initial-fit while wrappers are 0×0). Added a regression-guard test asserting every node position and `totalHeight` is finite/non-negative and edges reference real nodes. Verified: `npx tsc --noEmit` green; `npx vitest run src/components/usage/providerModelGraph.test.ts` 6/6 passed. | `web/src/components/usage/{ProviderModelGraphPanel.module.scss,ProviderModelGraphPanel.tsx,providerModelGraph.test.ts}`, `session.md` |
| 2026-08-03 | Reworked the provider↔model graph per user feedback: (1) model nodes are **merged by alias (fallback to name/label)**, so `gpt-5.5` shared across several providers becomes a single node with a multi-provider summary, eliminating the "model name too much" duplication; (2) layout switched from "one band per provider" (which made the canvas very tall and fitView zoom out too far) to a **compact two-column grid**: providers stack in a left column at `x=0` (54px rows + 24px gap), all unique models stack in a right column starting at `x=400` (42px rows), and only spill into up to 4 columns (380px apart) when there are more than 4 unique models, capping vertical height. `ProviderModelGraphGraph` now exposes `modelCount` and `totalWidth`; model datum carries `names[]`, `aliases[]`, `providers[]`, `providerCount`, `disabled`. Updated `ProviderModelGraphPanel` tooltips to render the merged provider/alias/name summary. Rewrote `providerModelGraph.test.ts` (4 tests) to pin merge-by-alias semantics, edge count per (provider, merged-model) pair, compact grid positions, and empty-graph handling. Verified: `npx tsc --noEmit` clean, `npx vitest run src/components/usage/providerModelGraph.test.ts` 4/4 passed. | `web/src/components/usage/{providerModelGraph.ts,providerModelGraph.test.ts,ProviderModelGraphPanel.tsx}`, `session.md` |

| 2026-08-03 | Migrated all architecture/spec/docs to specify provider-model relationships as a Graph Neural Network (GNN) rather than a static alias graph. Replaced all "provider model graph", "provider↔model graph", and related plain-graph language in `README.md`, `docs/ARCHITECTURE.md`, `docs/SPECIFICATION.md`, module/UI documentation, and feature summaries. Rationale: adopting a GNN foundation enables richer analytics (embeddings, prediction, structural learning), future-proofing for advanced features and setting up next-generation architecture. UI, backend, and API are now documented to surface, visualize, and analyze this GNN-based structure. | `README.md`, `docs/ARCHITECTURE.md`, `docs/SPECIFICATION.md`, `session.md` |
| 2026-08-03 (cont.) | **Implemented the GNN end-to-end** to match the docs. Backend: new `internal/service/provider_model_gnn.go` exposes `ProviderModelGNNProvider` returning `ProviderModelGraphResponse{providers, graph}` where `graph` carries `feature_names` (`[disabled, is_shared, degree_norm, sharing_degree_norm, rel_degree_norm, kind_hash]`), per-node 6-dim feature vectors, edge weights `1/totalEdges`, embeddings from one-layer mean message passing (`h_v' = 0.5·h_v + 0.5·mean(neighbors)`), and a meta block (`provider_count/model_count/edge_count/feature_dim/hidden_dim`). `NewProviderModelGraphService` retains as the constructor; route handler registers canonical `GET /api/provider-model-gnn` plus the legacy `/api/provider-model-graph` alias sharing one handler. Frontend: `web/src/lib/types.ts` gains GNN feature/graph/meta types; `api.fetchProviderModelGNN` replaces the old helper; `providerModelGraph.ts` builder joins named feature vectors, embeddings, and `kind_hash`/`is_shared` onto layout nodes; `ProviderModelGraphPanel` renamed to `ProviderModelGNNPanel` (matches docs) and now tints provider nodes by `kind_hash` hue, adds a shared-model badge driven by `is_shared=1`, surfaces feature/embedding lines in tooltips, and shows a dims-aware summary chip. i18n updated in en/zh/zh-TW with `tooltip_models/features/embedding`, `shared_badge`, and a `{{dims}}` placeholder in `summary`. ARCHITECTURE.md gained a paragraph describing the UI-visible GNN behaviors so docs and code agree. Verified: `go build ./...`, `go test ./internal/service/ ./internal/api/`, `npx tsc --noEmit`, `npx eslint src --max-warnings=0`, `npx vitest run` (109 files / 990 tests, including the 6 rewritten `providerModelGraph.test.ts` cases pinning GNN joins). | `internal/service/provider_model_gnn.go`, `internal/api/{provider_model_graph,router}.go`, `web/src/lib/{api,types}.ts`, `web/src/components/usage/{ProviderModelGNNPanel.tsx,ProviderModelGNNPanel.module.scss,providerModelGraph.ts,providerModelGraph.test.ts,index.ts}`, `web/src/pages/UsagePage.tsx`, `web/src/i18n/index.ts`, `docs/ARCHITECTURE.md`, `session.md` |
| 2026-08-03 (cont.) | **Added a reagraph (WebGL/three.js) second renderer for the GNN panel** per user request "Try to use react library reagraph network graph visualization." New `ProviderModelReagraphPanel.tsx` exports a canvas-only `ProviderModelReagraphCanvas({graph})` (receives the already-fetched layout from the host) plus a standalone `ProviderModelReagraphPanel()` that fetches itself; maps `buildProviderModelGraph` output → `GraphNode[]`/`GraphEdge[]` (provider fill from `kind_hash` hue with theme-aware alpha, model fill by `is_shared`, provider size ∝ model count [8→32], model size ∝ provider count [6→18]) and renders `GraphCanvas` with `layoutType='forceDirected2d'`, `draggable`, `animated`, `sizingType='attribute'`, curved edges, hover tooltips that surface feature/embedding text via a MutationObserver watching `<html data-theme>`. `ProviderModelGNNPanel` gains a `renderer: 'flow' \| 'reagraph'` toggle in the header (i18n keys `renderer_label/classic/reagraph` in en/zh/zh-TW) and lazy-imports the canvas via `lazy(() => import('./ProviderModelReagraphPanel'))` so the three.js bundle (~380 kB gzip) splits into `ProviderModelReagraphPanel-*.js` and only downloads when the user picks the network view. The standalone reagraph panel is intentionally *not* re-exported from `components/usage/index.ts` to avoid an `[INEFFECTIVE_DYNAMIC_IMPORT]` chunk-merge. Fixed two follow-ups surfaced by `npm run lint`: (a) removed the synchronous `setLoading(true)/setError(null)` from the effect body (already covered by `useState(true)`/`useState(null)`, fixes `react-hooks/set-state-in-effect`); (b) tooltip payload read as a typed cast of `node.data as unknown as ReagraphNodeData` with a `{kindLabel:''}` fallback. Docs: ARCHITECTURE.md §7 rewritten to describe the dual renderer and lazy-loading decision; session.md updated. Verified: `npm run typecheck` clean, `npm run lint` clean, `npm run test` 990/990, `npm run build` green — dist shows `index-CTrtP69A.js` at 974 kB (main) vs `ProviderModelReagraphPanel-*.js` at 1,345 kB (WebGL chunk, lazy). | `web/src/components/usage/{ProviderModelGNNPanel.tsx,ProviderModelGNNPanel.module.scss,ProviderModelReagraphPanel.tsx,ProviderModelReagraphPanel.module.scss,index.ts}`, `web/src/i18n/index.ts`, `web/package.json`, `docs/ARCHITECTURE.md`, `session.md` |
| 2026-08-03 | Made the reagraph GNN canvas **theme-adaptive** (user report: white background lingered in dark theme → harsh contrast). Root cause: `ProviderModelReagraphCanvas` never passed a `theme` prop, so reagraph's default `lightTheme.canvas.background` (white) kept painting the opaque WebGL canvas regardless of `data-theme`. Fix: added `buildGraphTheme(isDark)` in `ProviderModelReagraphPanel.tsx` that spreads the matching reagraph `lightTheme`/`darkTheme` preset and pins `canvas.background` (`#f0eee8` light / `#1d1b18` dark), node+edge label and sub-label colors/strokes, and edge/arrow fills to the exact `--bg-primary`/`--text-*`/`--border-color` hexes from `themes.scss`; the memoized `Theme` feeds `<GraphCanvas theme={graphTheme}>` and flips with the existing `isDark` MutationObserver state, so the background and all elements track the active theme. Verified: `npm run typecheck` clean, `npm run lint` clean, `npm run test` 109 files / 990 tests green, `npm run build` green with the lazy reagraph chunk preserved. Docs: ARCHITECTURE.md §7 theme-adaptive note. | `web/src/components/usage/ProviderModelReagraphPanel.tsx`, `docs/ARCHITECTURE.md`, `session.md` |
